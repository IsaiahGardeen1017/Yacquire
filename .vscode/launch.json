{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug Server (compiled JS)",
            "program": "${workspaceFolder}/build/server/src/server/main.js", // or wherever your compiled entry is
            "cwd": "${workspaceFolder}",
            "autoAttachChildProcesses": true
        }
    ]
}